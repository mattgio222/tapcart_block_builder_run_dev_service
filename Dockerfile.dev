# Dockerfile for Tapcart dev container
# Pre-installs the Tapcart CLI for faster session startup

FROM node:20-slim

# Install Tapcart CLI globally
RUN npm install -g @tapcart/tapcart-cli

WORKDIR /app

# Default command - will be overridden by the orchestrator
CMD ["tapcart", "block", "dev"]
